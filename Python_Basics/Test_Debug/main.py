def total_xp(level, xp_to_add):
    # Convert the current level to xp
    current_xp = level * 100
    
    # Add the current xp to xp_to_add
    total_xp = current_xp + xp_to_add
    
    return total_xp
